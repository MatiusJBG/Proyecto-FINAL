import React from 'react';

function StudentManager() {
  // Aquí iría la lógica de gestión de alumnos (tabla, CRUD, etc.)
  return (
    <div className="admin-section">
      <h3>Gestión de Alumnos</h3>
      <div style={{color:'#bfc9d1',padding:24}}>Aquí puedes gestionar los alumnos (CRUD, tabla, etc.).</div>
    </div>
  );
}

export default StudentManager;
